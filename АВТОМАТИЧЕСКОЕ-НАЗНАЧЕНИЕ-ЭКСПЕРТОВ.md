# Автоматическое назначение статуса эксперта при оплате

## 🎯 Что было сделано

Улучшена система автоматического назначения статуса эксперта пользователям, которые успешно оплатили **месячную (monthly)** или **годовую (yearly)** подписку через Юкассу.

## ✅ Изменения в коде

### 1. Улучшена функция `processSuccessfulPayment` в `backend/src/routes/payments.ts`

**Что изменилось:**
- ✅ Добавлена явная проверка типа подписки (monthly/yearly)
- ✅ Добавлено подробное логирование всех операций
- ✅ Улучшена обработка ошибок
- ✅ Добавлена проверка текущего статуса пользователя перед обновлением
- ✅ Уведомления теперь содержат информацию о типе подписки

**Логика работы:**
```
1. Webhook от Юкассы → событие payment.succeeded
2. Ищем платеж в базе данных по yookassa_payment_id
3. Проверяем, что plan_id = 'monthly' или 'yearly'
4. Обновляем user_type = 'expert' для пользователя
5. Отправляем уведомление пользователю
```

### 2. Улучшен webhook handler

**Добавлено:**
- Подробное логирование всех входящих webhook от Юкассы
- Вывод всех данных платежа для диагностики
- Информация о времени получения webhook
- Детальные сообщения об ошибках

**Пример лога:**
```
=====================================
📥 Получен webhook от Юкассы
Время: 2025-11-29T10:00:00.000Z
📋 Событие: payment.succeeded
📋 ID платежа Юкассы: 12345-67890-abc
📋 Статус: succeeded
🔍 Ищем платеж в базе данных...
✅ Платеж найден в БД: ID 123, user_id: 45, plan_id: yearly
✅ Пользователь user@example.com (ID: 45) успешно стал экспертом после оплаты плана yearly
✅ Webhook обработан успешно
=====================================
```

### 3. Улучшен fallback механизм

На случай, если webhook не сработал, добавлен механизм проверки статуса платежа напрямую в Юкассе при запросе `/api/payments/status/:paymentId`.

## 📋 Проверка работы системы

### Шаг 1: Проверьте текущую ситуацию в базе данных

Запустите SQL скрипт для диагностики:

```bash
psql -d ваша_база_данных -f FIX-PAYMENTS-AUTO-EXPERT.sql
```

Или в pgAdmin выполните содержимое файла `FIX-PAYMENTS-AUTO-EXPERT.sql`.

**Что покажет скрипт:**
- Все платежи и статусы пользователей
- Проблемные случаи (успешный платеж, но пользователь не эксперт)
- Статистику по платежам
- Финальную проверку корректности

### Шаг 2: Исправьте существующие проблемы (если есть)

Если скрипт показал проблемные платежи:

1. Откройте файл `FIX-PAYMENTS-AUTO-EXPERT.sql`
2. Раскомментируйте блок DO $$ (строки 61-97)
3. Запустите скрипт снова

Это автоматически:
- Обновит статус всех пользователей с успешными monthly/yearly платежами на 'expert'
- Отправит им уведомления
- Покажет количество обновленных пользователей

### Шаг 3: Перезапустите backend

После изменений в коде обязательно перезапустите backend:

```bash
# Если используете Docker
docker-compose restart backend

# Или если запускаете локально
cd backend
npm run build
npm start
```

### Шаг 4: Проверьте логи backend

После перезапуска следите за логами:

```bash
# Если используете Docker
docker logs -f synergy_backend

# Или если запускаете локально
# Логи будут в консоли где запущен npm start
```

**Что искать в логах:**
- `📥 Получен webhook от Юкассы` - webhook приходит
- `✅ Платеж найден в БД` - платеж найден в базе
- `✅ План monthly/yearly дает статус эксперта` - план проверяется
- `✅ Пользователь ... успешно стал экспертом` - статус обновлен

## 🧪 Тестирование

### 1. Тестовый платеж

Создайте тестовый платеж через интерфейс:

1. Зарегистрируйте нового пользователя
2. Перейдите на страницу "Стать экспертом"
3. Выберите месячную или годовую подписку
4. Используйте тестовую карту: **5555 5555 5555 4444**
5. CVV: **123**, срок: любая будущая дата
6. Завершите оплату

### 2. Проверьте результат

**В логах backend должно появиться:**
```
📥 Получен webhook от Юкассы
✅ Пользователь test@example.com стал экспертом после оплаты плана monthly
```

**В базе данных:**
```sql
SELECT u.email, u.user_type, p.plan_id, p.status
FROM users u
JOIN payments p ON p.user_id = u.id
WHERE u.email = 'test@example.com';
```

Должно показать:
- user_type = 'expert'
- plan_id = 'monthly' или 'yearly'
- status = 'succeeded'

## 🔧 Настройка webhook в Юкассе

Если webhook не приходят, проверьте настройки в личном кабинете Юкассы:

1. Войдите в [yookassa.ru](https://yookassa.ru/)
2. Откройте раздел **"Настройки"** → **"Webhook"**
3. Проверьте URL: `https://ваш-домен.ru/api/payments/webhook`
4. Убедитесь, что выбрано событие: `payment.succeeded`
5. Проверьте, что webhook активен

## ❓ Решение проблем

### Проблема: Webhook не приходят

**Решение:**
1. Проверьте настройки webhook в Юкассе
2. Убедитесь, что URL доступен извне
3. Проверьте логи nginx/apache на наличие запросов от Юкассы
4. Временно можно использовать fallback механизм (автоматическая проверка статуса)

### Проблема: Webhook приходит, но пользователь не становится экспертом

**Решение:**
1. Проверьте логи backend на наличие ошибок
2. Убедитесь, что в таблице users есть constraint для user_type:
   ```sql
   SELECT conname, pg_get_constraintdef(oid) 
   FROM pg_constraint 
   WHERE conrelid = 'users'::regclass;
   ```
3. Если constraint не включает 'expert', выполните:
   ```sql
   ALTER TABLE users DROP CONSTRAINT IF EXISTS users_user_type_check;
   ALTER TABLE users ADD CONSTRAINT users_user_type_check 
   CHECK (user_type IN ('client', 'expert', 'admin'));
   ```

### Проблема: Платеж не найден в базе данных

**Решение:**
1. Убедитесь, что платеж создается через `/api/payments/create`
2. Проверьте, что yookassa_payment_id сохраняется в таблице payments
3. Проверьте логи создания платежа

## 📊 Мониторинг

Регулярно запускайте проверочный запрос:

```sql
-- Показать все необработанные успешные платежи
SELECT 
    p.id, p.plan_id, p.amount, u.email, u.user_type
FROM payments p
JOIN users u ON p.user_id = u.id
WHERE p.status = 'succeeded' 
  AND p.plan_id IN ('monthly', 'yearly')
  AND u.user_type != 'expert';
```

Если этот запрос возвращает строки - значит есть проблема с автоматизацией.

## 📝 Примечания

1. **Lifetime подписка** не дает автоматический статус эксперта через webhook (можно добавить при необходимости)
2. **Free план** не дает статус эксперта
3. Все изменения статуса логируются с отметкой времени
4. Пользователи получают уведомление после назначения статуса эксперта

## 🚀 Что дальше?

После успешного тестирования рекомендуется:

1. Настроить мониторинг логов (например, через Sentry или аналог)
2. Добавить email-уведомления об успешной оплате
3. Настроить автоматическое продление подписок
4. Добавить отображение даты окончания подписки в профиле

---

**Дата обновления:** 29 ноября 2025  
**Версия:** 2.0









