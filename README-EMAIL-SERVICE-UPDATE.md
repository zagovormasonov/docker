# 📧 Переход на новый EmailJS сервис service_22cecl9

> **TL;DR:** Запустите `setup-email-service.bat` (Windows) или `./setup-email-service.sh` (Linux/Mac) и следуйте инструкциям.

---

## 🎯 Задача

Обновить систему для использования нового EmailJS сервиса `service_22cecl9` для всех email рассылок:
- ✉️ Регистрация новых пользователей (подтверждение email)
- 🔑 Восстановление пароля

## ✅ Что уже готово

Все файлы конфигурации и кода готовы к использованию нового сервиса:
- Код использует переменные окружения
- Документация обновлена
- Созданы шаблоны .env файлов
- Созданы скрипты автоматической настройки

**ID шаблонов остаются прежними** - меняется только Service ID!

## 🚀 Быстрый старт

### Вариант 1: Автоматическая настройка (рекомендуется)

**Windows:**
```cmd
setup-email-service.bat
```

**Linux/Mac:**
```bash
chmod +x setup-email-service.sh
./setup-email-service.sh
```

Скрипт создаст нужные .env файлы и покажет дальнейшие инструкции.

### Вариант 2: Ручная настройка

<details>
<summary><b>Для локальной разработки (кликните чтобы развернуть)</b></summary>

```bash
# 1. Создайте .env файл
cp frontend/env.example frontend/.env

# 2. Откройте frontend/.env и замените:
# - VITE_EMAILJS_PUBLIC_KEY=ваш_ключ
# - VITE_EMAILJS_VERIFICATION_TEMPLATE_ID=ваш_template_id
# - VITE_EMAILJS_RESET_PASSWORD_TEMPLATE_ID=ваш_template_id
# (Service ID уже установлен на service_22cecl9)

# 3. Перезапустите dev сервер
cd frontend
npm run dev
```
</details>

<details>
<summary><b>Для продакшена с Docker (кликните чтобы развернуть)</b></summary>

```bash
# 1. Создайте .env.prod файл
cp env.prod.example .env.prod

# 2. Откройте .env.prod и замените ВСЕ значения your_* на реальные
# (включая EmailJS переменные, Service ID уже установлен)

# 3. Пересоберите и запустите Docker
docker-compose -f docker-compose.prod.yml down
docker-compose -f docker-compose.prod.yml --env-file .env.prod build --no-cache frontend
docker-compose -f docker-compose.prod.yml --env-file .env.prod up -d
```
</details>

## 📋 Где взять значения для .env

Зайдите на https://dashboard.emailjs.com:

| Переменная | Где найти | Статус |
|-----------|-----------|--------|
| `VITE_EMAILJS_SERVICE_ID` | Уже установлен | ✅ `service_22cecl9` |
| `VITE_EMAILJS_PUBLIC_KEY` | Account → General → Public Key | ⚙️ Ваше значение |
| `VITE_EMAILJS_VERIFICATION_TEMPLATE_ID` | Email Templates → Template ID | ⚙️ Остается прежним |
| `VITE_EMAILJS_RESET_PASSWORD_TEMPLATE_ID` | Email Templates → Template ID | ⚙️ Остается прежним |

## 🧪 Как проверить

1. **Регистрация:**
   - Зарегистрируйтесь с новым email
   - Проверьте почту → должно прийти письмо

2. **Восстановление пароля:**
   - Нажмите "Забыли пароль?"
   - Введите email
   - Проверьте почту → должно прийти письмо

3. **В консоли браузера (F12):**
   ```javascript
   console.log(import.meta.env.VITE_EMAILJS_SERVICE_ID);
   // Должно вывести: service_22cecl9
   ```

## 📍 Технические детали

### Где используется новый сервис:
- `frontend/src/pages/RegisterPage.tsx:21` - регистрация
- `frontend/src/pages/ForgotPasswordPage.tsx:19` - восстановление пароля

### Как работает:
1. Код: `import.meta.env.VITE_EMAILJS_SERVICE_ID`
2. При сборке → заменяется значением из .env
3. Для Docker → передается через build args

### Что НЕ меняется:
- ✅ ID шаблонов остаются прежними
- ✅ Код остается без изменений
- ✅ Структура писем не меняется

## 📚 Документация

Выберите нужный уровень детализации:

| Файл | Описание | Для кого |
|------|----------|----------|
| `README-EMAIL-SERVICE-UPDATE.md` | 👈 Вы здесь | Все |
| `QUICK-START-NEW-EMAIL-SERVICE.md` | Быстрый старт | Разработчики |
| `ОБНОВЛЕНИЕ-EMAIL-СЕРВИСА.md` | На русском, подробно | Русскоязычные |
| `EMAILJS-SERVICE-UPDATE.md` | Полная техническая документация | DevOps |
| `PRODUCTION-SETUP.md` | Настройка продакшена | DevOps |

## ❗ Важно

1. **Не коммитьте .env файлы!** Они в `.gitignore`
2. **Перезапускайте после изменений** - переменные встраиваются при сборке
3. **Для Docker нужна пересборка** - используйте `--no-cache`

## 🐛 Проблемы?

<details>
<summary><b>Письма не приходят</b></summary>

- ✅ Проверьте правильность всех переменных
- ✅ Проверьте квоту EmailJS
- ✅ Проверьте spam в почте
- ✅ Посмотрите консоль браузера (F12)
</details>

<details>
<summary><b>Переменные не применяются</b></summary>

- ✅ .env файл в правильной папке?
- ✅ Перезапустили dev сервер?
- ✅ Для Docker: пересобрали с `--no-cache`?
</details>

<details>
<summary><b>Docker не запускается</b></summary>

```bash
# Посмотрите логи
docker logs synergy-frontend

# Проверьте переменные
docker exec synergy-frontend env | grep VITE_EMAILJS
```
</details>

## 🎓 Дополнительная помощь

Подробные инструкции по устранению проблем и расширенные настройки смотрите в:
- `EMAILJS-SERVICE-UPDATE.md` - полная техническая документация
- `ОБНОВЛЕНИЕ-EMAIL-СЕРВИСА.md` - на русском языке

---

## ✨ Итого

### Что нужно сделать:
1. ✅ Запустить `setup-email-service.bat/.sh` ИЛИ скопировать .env вручную
2. ✅ Заменить значения `your_*` на реальные
3. ✅ Перезапустить сервер/Docker
4. ✅ Проверить работу

### Что уже готово:
- ✅ Service ID установлен на `service_22cecl9`
- ✅ Код обновлен для использования переменных окружения
- ✅ Документация создана
- ✅ Скрипты настройки готовы

**Время на настройку: ~5 минут**

---

**Вопросы?** Смотрите подробные инструкции в файлах документации выше.






