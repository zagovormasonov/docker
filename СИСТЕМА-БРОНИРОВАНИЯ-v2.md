# 📅 Система бронирования v2.0 - Расписание по дням недели

## 🎉 Что нового в версии 2.0

**Основное изменение:** Эксперты теперь настраивают постоянное расписание работы по дням недели, вместо добавления конкретных дат!

### Преимущества новой системы:

✅ **Для экспертов:**
- Настройка расписания один раз
- Автоматическая генерация доступных слотов
- Не нужно каждый раз добавлять новые даты
- Гибкость: разное расписание для разных дней недели
- Выбор длительности слота (30мин / 1час / 1.5часа / 2часа)

✅ **Для клиентов:**
- Видят доступные даты на месяц вперёд
- Интерфейс календаря остался прежним
- Сразу видно когда эксперт работает

## 🎯 Как это работает

### Для эксперта:

1. Заходит в **"Мой профиль"** → **"📅 Расписание"**
2. Выбирает:
   - **День недели** (понедельник, вторник и т.д.)
   - **Время начала** работы (например, 09:00)
   - **Время окончания** работы (например, 18:00)
   - **Длительность слота** (30мин / 1час / 1.5часа / 2часа)
3. Нажимает **"✓ Добавить расписание"**

**Пример:** "Каждый понедельник с 10:00 до 18:00, слоты по 1 часу"

### Для клиента:

1. Заходит на страницу эксперта
2. Видит календарь с доступными датами на месяц вперёд
3. Система автоматически генерирует слоты на основе расписания эксперта
4. Бронирует нужную дату и время

## 🗄️ Структура базы данных

### Новая таблица `expert_schedule`

Хранит постоянное расписание работы экспертов:

```sql
CREATE TABLE expert_schedule (
  id SERIAL PRIMARY KEY,
  expert_id INTEGER REFERENCES users(id),
  day_of_week INTEGER NOT NULL CHECK (day_of_week >= 0 AND day_of_week <= 6),
  start_time TIME NOT NULL,
  end_time TIME NOT NULL,
  slot_duration INTEGER DEFAULT 60,
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(expert_id, day_of_week, start_time, end_time)
);
```

**Поля:**
- `day_of_week`: 0=воскресенье, 1=понедельник, ..., 6=суббота
- `start_time`: Время начала работы
- `end_time`: Время окончания работы
- `slot_duration`: Длительность одного слота в минутах (30, 60, 90, 120)
- `is_active`: Активно ли расписание

### Таблица `bookings` (без изменений)

Записи клиентов остались прежними - хранят конкретные даты и время.

## 🔌 Новые API Endpoints

### Для эксперта

#### Получить расписание
```http
GET /api/schedule/expert/schedule
Authorization: Bearer {token}
```

**Ответ:**
```json
[
  {
    "id": 1,
    "expert_id": 5,
    "day_of_week": 1,
    "start_time": "09:00:00",
    "end_time": "18:00:00",
    "slot_duration": 60,
    "is_active": true
  }
]
```

#### Добавить расписание
```http
POST /api/schedule/expert/schedule
Authorization: Bearer {token}
Content-Type: application/json

{
  "dayOfWeek": 1,
  "startTime": "09:00",
  "endTime": "18:00",
  "slotDuration": 60
}
```

**Параметры:**
- `dayOfWeek`: 0-6 (0=воскресенье, 1=понедельник, ..., 6=суббота)
- `startTime`: Время в формате "HH:MM"
- `endTime`: Время в формате "HH:MM"
- `slotDuration`: 30 | 60 | 90 | 120 (минуты)

#### Удалить расписание
```http
DELETE /api/schedule/expert/schedule/:id
Authorization: Bearer {token}
```

### Для клиента

#### Получить доступные слоты на основе расписания
```http
GET /api/schedule/expert/:expertId/available-slots?startDate=2025-11-01&daysAhead=30
Authorization: Bearer {token}
```

**Параметры:**
- `startDate`: С какой даты генерировать слоты (опционально, по умолчанию сегодня)
- `daysAhead`: На сколько дней вперёд (опционально, по умолчанию 30)
- `endDate`: До какой даты (опционально, альтернатива daysAhead)

**Ответ:**
```json
[
  {
    "date": "2025-11-18",
    "time_slot": "10:00",
    "day_of_week": 1,
    "is_available": true
  },
  {
    "date": "2025-11-18",
    "time_slot": "11:00",
    "day_of_week": 1,
    "is_available": true
  }
]
```

#### Бронирование (без изменений)
```http
POST /api/bookings/book
Authorization: Bearer {token}
Content-Type: application/json

{
  "expertId": 5,
  "date": "2025-11-18",
  "time_slot": "10:00",
  "clientMessage": "Хочу проконсультироваться..."
}
```

## 📱 Интерфейс

### Эксперт - Настройка расписания

```
┌─────────────────────────────────────────┐
│ 📅 Управление записями                   │
├─────────────────────────────────────────┤
│ [📅 Расписание] [📋 Записи (2)]         │
├─────────────────────────────────────────┤
│                                          │
│ Настройте график работы                  │
│ Выберите дни недели и время              │
│                                          │
│ День недели: [Понедельник ▼]            │
│                                          │
│ Время начала: [09:00]                    │
│ Время окончания: [18:00]                 │
│                                          │
│ Длительность слота: [1 час ▼]           │
│                                          │
│ [✓ Добавить расписание]                 │
│                                          │
├─────────────────────────────────────────┤
│ Ваше расписание:                         │
│                                          │
│ Понедельник                              │
│  🕐 09:00 - 18:00  📊 Слот: 60 мин  [✕] │
│                                          │
│ Среда                                    │
│  🕐 10:00 - 16:00  📊 Слот: 30 мин  [✕] │
│                                          │
└─────────────────────────────────────────┘
```

### Клиент - Календарь бронирования

Интерфейс остался прежним! Клиент видит обычный календарь с доступными датами и временем.

## 🚀 Установка и обновление

### Если у вас уже есть старая версия:

```bash
# 1. Применить SQL миграцию
psql -U your_username -d your_database -f CREATE-BOOKINGS-TABLES.sql

# 2. Перезапустить backend
cd backend && npm run dev

# 3. Перезапустить frontend  
cd frontend && npm run dev
```

### Новая установка:

Таблицы создадутся автоматически при первом запуске сервера!

```bash
# Backend
cd backend && npm run dev

# Frontend
cd frontend && npm run dev
```

## 🔄 Миграция данных (опционально)

Если у экспертов уже есть конкретные даты в старой системе, можно продолжать их использовать. Новая система расписания работает параллельно.

Или можно пересоздать расписание:
1. Эксперт удаляет старые конкретные даты
2. Добавляет постоянное расписание по дням недели

## 💡 Примеры использования

### Пример 1: Эксперт работает по будням

```javascript
// Понедельник-Пятница, 9:00-18:00, слоты по 1 часу
for (let day = 1; day <= 5; day++) {
  await axios.post('/schedule/expert/schedule', {
    dayOfWeek: day,
    startTime: '09:00',
    endTime: '18:00',
    slotDuration: 60
  });
}
```

### Пример 2: Разное расписание

```javascript
// Понедельник: 10:00-14:00 (короткий день)
await axios.post('/schedule/expert/schedule', {
  dayOfWeek: 1,
  startTime: '10:00',
  endTime: '14:00',
  slotDuration: 60
});

// Среда: 12:00-20:00 (вечерний прием)
await axios.post('/schedule/expert/schedule', {
  dayOfWeek: 3,
  startTime: '12:00',
  endTime: '20:00',
  slotDuration: 90 // Длинные сессии
});
```

## 📊 Логика генерации слотов

Backend автоматически:

1. Берёт расписание эксперта из `expert_schedule`
2. Генерирует конкретные даты на N дней вперёд
3. Разбивает каждый день на слоты по заданной длительности
4. Исключает уже забронированные слоты
5. Возвращает доступные слоты клиенту

**Пример:**
- Расписание: Понедельник 10:00-13:00, слоты по 1 часу
- Генерация на 18.11.2025 (понедельник):
  - 10:00 ✅
  - 11:00 ❌ (уже забронировано)
  - 12:00 ✅

## 🎨 Особенности UI

- ✅ Фиолетовые акценты (#9333ea)
- ✅ Удобные селекты для выбора дня недели
- ✅ Time picker для времени
- ✅ Красивое отображение расписания по дням
- ✅ Адаптивный дизайн для мобильных
- ✅ Поддержка темной темы

## 🔧 Конфигурация

### Длительности слотов (можно изменить в ExpertCalendar.tsx):

```typescript
const SLOT_DURATIONS = [
  { value: 30, label: '30 минут' },
  { value: 60, label: '1 час' },
  { value: 90, label: '1.5 часа' },
  { value: 120, label: '2 часа' }
];
```

### Дни недели:

```typescript
const DAYS_OF_WEEK = [
  { value: 1, label: 'Понедельник' },
  { value: 2, label: 'Вторник' },
  { value: 3, label: 'Среда' },
  { value: 4, label: 'Четверг' },
  { value: 5, label: 'Пятница' },
  { value: 6, label: 'Суббота' },
  { value: 0, label: 'Воскресенье' }
];
```

## 📝 Changelog

### v2.0 (16.11.2025)
- ✅ Добавлена таблица `expert_schedule` для расписания по дням недели
- ✅ Новый API `/api/schedule/*` для управления расписанием
- ✅ Автоматическая генерация слотов на основе расписания
- ✅ Обновлён интерфейс эксперта (выбор дней недели)
- ✅ Выбор длительности слота (30/60/90/120 минут)
- ✅ Сохранена обратная совместимость

### v1.0 (16.11.2025)
- Система бронирования с конкретными датами

## 🐛 Troubleshooting

### Слоты не генерируются
- Проверьте, что эксперт добавил расписание
- Проверьте таблицу `expert_schedule` в БД
- Убедитесь что `is_active = true`

### Ошибка "Время начала должно быть раньше времени окончания"
- Проверьте правильность ввода времени
- Время начала должно быть < времени окончания

## 🎯 Roadmap

- [ ] Исключения из расписания (отпуск, выходные)
- [ ] Массовое копирование расписания на несколько дней
- [ ] Повторяющиеся исключения
- [ ] Разные цены для разных слотов

---

**Версия:** 2.0.0  
**Дата:** 16 ноября 2025  
**Автор:** AI Assistant  
**Статус:** ✅ Готово к использованию

