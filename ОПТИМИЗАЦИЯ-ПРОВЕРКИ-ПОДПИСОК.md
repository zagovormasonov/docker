# 🚀 Оптимизация проверки подписок

## ✅ Что изменено

### Было (неоптимально):
- ❌ Проверка каждые 5 минут при каждом запросе пользователя
- ❌ Высокая нагрузка на сервер и БД
- ❌ Уведомление только после истечения подписки

### Стало (оптимизировано):
- ✅ Проверка **1 раз в сутки** в 03:00 ночи (минимальная нагрузка)
- ✅ Предупреждения отправляются **за 5 дней до истечения**
- ✅ Использование node-cron для планирования задач
- ✅ Минимальная нагрузка на сервер

---

## ⏰ Новое расписание

### Ежедневная проверка в 03:00
Каждую ночь в 03:00 автоматически выполняется:

1. **Отправка предупреждений** (за 5 дней до истечения)
   - Проверяются подписки, истекающие в ближайшие 5 дней
   - Отправляются уведомления пользователям
   - Уведомления не повторяются чаще 1 раза в 24 часа

2. **Отзыв истекших подписок**
   - Автоматическое снятие статуса эксперта
   - Отправка уведомлений об истечении
   - Изменение `user_type` с `expert` на `client`

---

## 📋 Установка

### Шаг 1: Установите node-cron

```bash
cd backend
npm install node-cron@^3.0.3
npm install --save-dev @types/node-cron@^3.0.11
```

### Шаг 2: Перезапустите backend

```bash
# Через Docker
docker-compose restart backend

# Или локально
npm run build
npm start
```

### Шаг 3: Готово! ✅

Cron job запустится автоматически при старте сервера.

---

## 📨 Система уведомлений

### 1. Предупреждение за 5 дней

**Условия отправки:**
- Подписка истекает через 5 дней или меньше
- Подписка еще активна (не истекла)
- Не отправляли уведомление за последние 24 часа

**Текст уведомления:**
```
⚠️ Напоминание: Ваша [месячная/годовая] подписка истекает через X дней (дата). 
Рекомендуем продлить подписку заранее.
```

### 2. Уведомление за 1 день

**Текст уведомления:**
```
⚠️ Внимание! Ваша [месячная/годовая] подписка истекает завтра (дата). 
Продлите подписку, чтобы не потерять доступ к функциям эксперта.
```

### 3. Уведомление об истечении

**Отправляется когда подписка истекла:**
```
Ваша [месячная/годовая] подписка истекла [дата]. 
Для продолжения работы эксперта, пожалуйста, продлите подписку.
```

---

## 🛠️ API для ручного управления

### 1. Ручная проверка истекших подписок

```bash
POST /api/subscriptions/check-expired
Authorization: Bearer <admin-token>
```

**Ответ:**
```json
{
  "message": "Проверка завершена",
  "revokedCount": 2,
  "revokedUsers": [
    {
      "id": 123,
      "email": "user@example.com",
      "username": "User Name"
    }
  ]
}
```

### 2. Ручная отправка предупреждений

```bash
POST /api/subscriptions/send-warnings
Authorization: Bearer <admin-token>
```

**Ответ:**
```json
{
  "message": "Предупреждения отправлены",
  "warningCount": 5,
  "warnedUsers": [
    {
      "id": 456,
      "email": "expert@example.com",
      "username": "Expert Name"
    }
  ]
}
```

### 3. Полная проверка (предупреждения + истекшие)

```bash
POST /api/subscriptions/daily-check
Authorization: Bearer <admin-token>
```

**Ответ:**
```json
{
  "message": "Полная проверка завершена успешно"
}
```

---

## 📊 Логи

При каждой проверке в консоль выводятся детальные логи:

```
🌙 ==========================================
🌙 ЕЖЕДНЕВНАЯ ПРОВЕРКА ПОДПИСОК
🌙 Время: 2025-11-30T03:00:00.000Z
🌙 ==========================================

📨 Шаг 1: Отправка предупреждений...
⚠️ Проверка подписок, истекающих в ближайшие 5 дней...
📋 Найдено пользователей с истекающей подпиской: 3
✅ Предупреждение отправлено: user1@example.com (осталось 5 дней)
✅ Предупреждение отправлено: user2@example.com (осталось 3 дня)
✅ Предупреждение отправлено: user3@example.com (осталось 1 день)
✅ Отправлено 3 предупреждений

🔄 Шаг 2: Отзыв истекших подписок...
🔍 Проверка истекших подписок...
📋 Найдено пользователей с истекшей подпиской: 2
✅ Уведомление об истечении отправлено: expired1@example.com
✅ Уведомление об истечении отправлено: expired2@example.com
✅ Отозвано 2 истекших подписок

🌙 ==========================================
🌙 ПРОВЕРКА ЗАВЕРШЕНА УСПЕШНО
🌙 Предупреждений: 3
🌙 Отозвано: 2
🌙 ==========================================
```

---

## ⚙️ Настройка расписания

### Изменить время проверки

Откройте `backend/src/utils/subscription-cron.ts`:

```typescript
// По умолчанию: каждый день в 03:00
cronJob = cron.schedule('0 0 3 * * *', async () => {
  // ...
});

// Примеры других расписаний:
// '0 0 2 * * *'   - в 02:00 каждый день
// '0 0 4 * * *'   - в 04:00 каждый день
// '0 0 */12 * * *' - каждые 12 часов
// '0 0 0 * * 1'   - каждый понедельник в 00:00
```

### Изменить часовой пояс

```typescript
cronJob = cron.schedule('0 0 3 * * *', async () => {
  // ...
}, {
  scheduled: true,
  timezone: "Europe/Moscow" // Измените на свой часовой пояс
});
```

Примеры часовых поясов:
- `"Europe/Moscow"` - Москва (UTC+3)
- `"Europe/Kiev"` - Киев (UTC+2)
- `"Asia/Almaty"` - Алматы (UTC+6)
- `"Europe/Minsk"` - Минск (UTC+3)

### Изменить период предупреждений

В `backend/src/routes/subscription-checker.ts`:

```typescript
// По умолчанию: за 5 дней
AND u.subscription_expires_at < CURRENT_TIMESTAMP + INTERVAL '5 days'

// Можно изменить на:
// INTERVAL '7 days'  - за 7 дней
// INTERVAL '3 days'  - за 3 дня
// INTERVAL '10 days' - за 10 дней
```

---

## 🧪 Тестирование

### Тест 1: Проверить запуск cron job

При старте сервера должны увидеть:

```
⏰ ==========================================
⏰ ЗАПУСК CRON JOB ДЛЯ ПРОВЕРКИ ПОДПИСОК
⏰ Расписание: каждый день в 03:00
⏰ ==========================================

✅ Cron job для проверки подписок запущен успешно
📅 Следующий запуск: завтра в 03:00
```

### Тест 2: Ручной запуск проверки

```bash
curl -X POST http://localhost:5000/api/subscriptions/daily-check \
  -H "Authorization: Bearer <admin-token>"
```

### Тест 3: Проверить логи Docker

```bash
docker logs synergy-backend | grep "ЕЖЕДНЕВНАЯ ПРОВЕРКА"
```

---

## 📈 Преимущества новой системы

| Параметр | Было | Стало |
|----------|------|-------|
| Частота проверки | Каждые 5 минут | 1 раз в сутки |
| Нагрузка на БД | Высокая (постоянная) | Низкая (1 запрос/день) |
| Предупреждения | Нет | За 5 дней |
| Время проверки | В случайное время | В 03:00 (мин. нагрузка) |
| Нагрузка на сервер | ~288 проверок/день | 1 проверка/день |
| Повторы уведомлений | Возможны | Не чаще 1 раза в 24ч |

### Экономия ресурсов:
- **99.7% меньше** запросов к БД
- **Минимальная** нагрузка в пиковые часы
- **Предсказуемое** время выполнения
- **Умные** уведомления без спама

---

## 🐛 Устранение неполадок

### Cron job не запускается

1. Проверьте логи:
```bash
docker logs synergy-backend | grep "CRON"
```

2. Убедитесь, что установлен node-cron:
```bash
docker exec -it synergy-backend npm list node-cron
```

### Уведомления не отправляются

1. Проверьте таблицу notifications:
```sql
SELECT * FROM notifications 
WHERE type IN ('subscription_expiring', 'subscription_expired')
ORDER BY created_at DESC 
LIMIT 10;
```

2. Проверьте, есть ли подписки, истекающие в ближайшие 5 дней:
```sql
SELECT id, email, subscription_expires_at,
       EXTRACT(DAY FROM (subscription_expires_at - CURRENT_TIMESTAMP)) as days_left
FROM users
WHERE user_type = 'expert'
  AND subscription_expires_at < CURRENT_TIMESTAMP + INTERVAL '5 days'
  AND subscription_expires_at > CURRENT_TIMESTAMP;
```

### Проверка не выполняется в нужное время

Проверьте часовой пояс сервера:
```bash
docker exec -it synergy-backend date
```

Если часовой пояс неправильный, измените в `subscription-cron.ts`:
```typescript
timezone: "Europe/Moscow" // Ваш часовой пояс
```

---

## ✅ Готово!

Теперь проверка подписок работает оптимально:
- ✅ 1 раз в день в 03:00
- ✅ Предупреждения за 5 дней
- ✅ Минимальная нагрузка
- ✅ Умные уведомления

**Полная документация:** `ИНСТРУКЦИЯ-ПОДПИСКИ-И-ЗАКРЕПЛЕНИЕ.md`









