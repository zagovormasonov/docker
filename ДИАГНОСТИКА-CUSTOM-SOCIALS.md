# 🔍 Диагностика: Дополнительные ссылки не отображаются

## 🚀 Шаги для диагностики

### 1. Перезапустите frontend

**Обязательно!** Изменения в коде не применятся без перезапуска.

```bash
# Если локально:
cd frontend
npm run dev

# Если в Docker:
docker-compose restart frontend
# или с пересборкой:
docker-compose up --build frontend
```

### 2. Откройте консоль браузера (F12)

### 3. Откройте профиль эксперта

Перейдите на страницу любого эксперта

### 4. Проверьте загрузку данных

В консоли должно появиться:
```
Custom socials loaded: [{id: 1, name: "VK", url: "https://vk.com/..."}, ...]
```

**Если не появилось:**
- API не возвращает данные
- Эксперт не добавлял дополнительные ссылки

### 5. Нажмите кнопку "Поделиться"

В консоли должно появиться:
```
Opening share modal with customSocials: [{...}]
ShareProfileModal opened with expert: {id: 21, name: "...", customSocials: [...]}
Custom socials: [{...}]
```

**Если `customSocials: []` или `undefined`:**
- Данные не передаются в модалку

## 🧪 Тест 1: Есть ли дополнительные ссылки у эксперта?

**В консоли браузера выполните:**

```javascript
// Замените 21 на ID эксперта
fetch('https://soulsynergy.ru/api/users/custom-socials/21', {
  headers: {
    'Authorization': `Bearer ${localStorage.getItem('token')}`
  }
})
.then(r => r.json())
.then(data => console.log('Custom socials from API:', data));
```

**Ожидаемый результат:**
```javascript
[
  {id: 1, name: "VK", url: "https://vk.com/page", created_at: "..."},
  {id: 2, name: "YouTube", url: "https://youtube.com/...", created_at: "..."}
]
```

**Если пустой массив `[]`:**
- У эксперта нет дополнительных ссылок
- Добавьте их в профиле!

## 🧪 Тест 2: Добавьте дополнительные ссылки

### В своём профиле:

1. Откройте "Мой профиль"
2. Прокрутите до раздела соцсетей
3. Нажмите "Добавить соцсеть" (или аналогичную кнопку)
4. Добавьте:
   - Название: `VK`
   - URL: `https://vk.com/mypage`
5. Сохраните
6. Добавьте ещё:
   - Название: `YouTube`
   - URL: `https://youtube.com/@mychannel`

### Проверьте снова:

1. Перезагрузите страницу (F5)
2. Нажмите "Поделиться профилем"
3. Проверьте консоль и модалку

## 🐛 Возможные проблемы

### Проблема 1: API не возвращает данные

**Проверка:**
```javascript
fetch('https://soulsynergy.ru/api/users/custom-socials/21')
.then(r => r.json())
.then(data => console.log(data));
```

**Если ошибка 404:**
- Backend роут не настроен
- Неверный ID пользователя

**Если пустой массив:**
- У пользователя нет дополнительных ссылок

### Проблема 2: Frontend не получает данные

**Проверка в консоли:**
```
Custom socials loaded: []  ← ПРОБЛЕМА!
```

**Решение:**
- Добавьте дополнительные ссылки в профиль
- Проверьте, что API возвращает данные

### Проблема 3: Данные не передаются в модалку

**Проверка в консоли:**
```
Opening share modal with customSocials: []  ← ПРОБЛЕМА!
```

**Решение:**
Проверьте код в `ExpertProfilePage.tsx`:

```typescript
<ShareProfileModal
  expert={{
    ...expert,
    customSocials: customSocials  // ← Эта строка должна быть!
  }}
/>
```

### Проблема 4: Модалка не отображает данные

**Проверка в консоли:**
```
ShareProfileModal opened with expert: {..., customSocials: [...]}
Custom socials: [{...}]  ← Данные есть, но не показываются
```

**Решение:**
Проверьте код в `ShareProfileModal.tsx`:

```typescript
{expert.customSocials && expert.customSocials.map((social) => (
  <Text key={social.id}>
    🔗 {social.name}: {social.url}
  </Text>
))}
```

## 📋 Чеклист отладки

- [ ] Frontend перезапущен
- [ ] Консоль браузера открыта (F12)
- [ ] У эксперта добавлены дополнительные ссылки
- [ ] API возвращает данные: `Custom socials loaded: [...]`
- [ ] Данные передаются: `Opening share modal with customSocials: [...]`
- [ ] Модалка получает данные: `Custom socials: [...]`
- [ ] В модалке видна секция "📞 Контакты"
- [ ] В контактах отображаются дополнительные ссылки

## 🆘 Быстрое решение

Если ничего не помогает, проверьте пошагово:

### Шаг 1: Добавьте тестовые данные

```sql
-- В базе данных (замените 21 на свой ID)
INSERT INTO custom_socials (user_id, name, url) 
VALUES 
  (21, 'VK', 'https://vk.com/test'),
  (21, 'YouTube', 'https://youtube.com/@test');
```

### Шаг 2: Проверьте в базе

```sql
SELECT * FROM custom_socials WHERE user_id = 21;
```

Должны быть записи.

### Шаг 3: Проверьте API

```bash
curl https://soulsynergy.ru/api/users/custom-socials/21
```

Должен вернуть JSON с данными.

### Шаг 4: Проверьте консоль

Откройте профиль → Консоль браузера → Должны быть логи.

## 💬 Напишите результаты

Скопируйте из консоли и отправьте:

1. `Custom socials loaded: ...`
2. `Opening share modal with customSocials: ...`
3. `Custom socials: ...`

Это поможет понять, на каком этапе проблема!

---

**Дата:** 28 ноября 2025








