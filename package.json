{
  "name": "synergy-platform",
  "version": "1.0.0",
  "description": "Платформа для духовных мастеров",
  "private": true,
  "workspaces": [
    "frontend",
    "frontend-test",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:frontend-test": "npm run dev --workspace=frontend-test",
    "dev:with-test": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:frontend-test\"",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend",
    "start:backend": "npm run start --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "ogl": "^1.0.11"
  }
}
