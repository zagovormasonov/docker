# 📧 Обновление Email Сервиса до service_22cecl9

## ✅ Что было сделано

Система настроена для использования нового EmailJS сервиса `service_22cecl9` для всех рассылок при регистрации и восстановлении пароля.

### Обновленные файлы:
1. ✅ `ENV-EXAMPLE.txt` - обновлен example конфигурации
2. ✅ `PRODUCTION-SETUP.md` - обновлена документация продакшена
3. ✅ `env.local.example` - создан шаблон для локальной разработки
4. ✅ `frontend/env.example` - создан шаблон для frontend
5. ✅ `env.prod.example` - создан шаблон для продакшена
6. ✅ `EMAILJS-SERVICE-UPDATE.md` - подробная инструкция
7. ✅ `QUICK-START-NEW-EMAIL-SERVICE.md` - быстрый старт

## 🎯 Что нужно сделать ВАМ

### Для локальной разработки:

```bash
# 1. Скопируйте шаблон
cp frontend/env.example frontend/.env

# 2. Откройте frontend/.env и замените значения:
# - VITE_EMAILJS_PUBLIC_KEY=ваш_ключ
# - VITE_EMAILJS_VERIFICATION_TEMPLATE_ID=ваш_id_шаблона
# - VITE_EMAILJS_RESET_PASSWORD_TEMPLATE_ID=ваш_id_шаблона
# - VITE_EMAILJS_MODERATION_TEMPLATE_ID=ваш_id_шаблона
# (Service ID уже установлен на service_22cecl9)

# 3. Перезапустите dev сервер
cd frontend
npm run dev
```

### Для продакшена (Docker):

```bash
# 1. Скопируйте шаблон
cp env.prod.example .env.prod

# 2. Откройте .env.prod и замените ВСЕ значения your_* на реальные
# Особенно важны EmailJS переменные (Service ID уже установлен)

# 3. Пересоберите Docker
docker-compose -f docker-compose.prod.yml down
docker-compose -f docker-compose.prod.yml --env-file .env.prod build --no-cache frontend
docker-compose -f docker-compose.prod.yml --env-file .env.prod up -d
```

## 📍 Где используется новый сервис

Новый сервис `service_22cecl9` будет автоматически использоваться в:
- ✉️ Регистрация новых пользователей (отправка письма с подтверждением)
- 🔑 Восстановление пароля (отправка письма со ссылкой)

### Файлы, которые используют новый сервис:
- `frontend/src/pages/RegisterPage.tsx` (строка 21)
- `frontend/src/pages/ForgotPasswordPage.tsx` (строка 19)

## ⚙️ Технические детали

### Как это работает:
1. Код использует переменную `import.meta.env.VITE_EMAILJS_SERVICE_ID`
2. При сборке эта переменная заменяется на значение из `.env` файла
3. Для Docker - значение передается через `docker-compose.prod.yml`

### ID шаблонов:
- **Остаются прежними!** Меняется только Service ID
- Для регистрации: `VITE_EMAILJS_VERIFICATION_TEMPLATE_ID`
- Для восстановления пароля: `VITE_EMAILJS_RESET_PASSWORD_TEMPLATE_ID`

## 🧪 Проверка работы

После настройки проверьте:

### 1. Регистрация пользователя
```
1. Откройте сайт → Регистрация
2. Заполните форму и отправьте
3. Проверьте почту - должно прийти письмо с подтверждением
```

### 2. Восстановление пароля
```
1. Откройте сайт → Вход → Забыли пароль?
2. Введите email и отправьте
3. Проверьте почту - должно прийти письмо со ссылкой
```

### 3. Проверка в консоли браузера (F12)
```javascript
// В консоли браузера выполните:
console.log(import.meta.env.VITE_EMAILJS_SERVICE_ID);
// Должно вывести: service_22cecl9
```

## ❗ Важные замечания

1. **Не коммитьте .env файлы в Git!** Они уже в `.gitignore`
2. **Перезапускайте сервер после изменения .env** - переменные встраиваются при сборке
3. **Для Docker нужна пересборка** - переменные встраиваются в образ при build
4. **Public Key и Template IDs берите из вашего аккаунта EmailJS**

## 📚 Где найти значения для .env

Зайдите на https://dashboard.emailjs.com:

1. **Service ID:** уже установлен `service_22cecl9` ✅
2. **Public Key:** Account → General → Public Key
3. **Template IDs:** Email Templates → выберите шаблон → скопируйте Template ID

## 🐛 Если что-то не работает

### Письма не приходят?
1. Проверьте правильность всех переменных в .env
2. Проверьте квоту EmailJS (может быть исчерпан лимит)
3. Проверьте spam/промоакции в почте
4. Посмотрите логи в консоли браузера (F12 → Console)

### Переменные не применяются?
1. Убедитесь что .env файл в правильной папке
2. Перезапустите dev сервер: `npm run dev`
3. Для Docker: пересоберите образ с `--no-cache`

### Docker проблемы?
```bash
# Посмотрите логи
docker logs synergy-frontend

# Проверьте переменные в контейнере
docker exec synergy-frontend env | grep VITE_EMAILJS
```

## 📖 Дополнительная документация

- **Быстрый старт:** `QUICK-START-NEW-EMAIL-SERVICE.md`
- **Подробная инструкция:** `EMAILJS-SERVICE-UPDATE.md`
- **Продакшн:** `PRODUCTION-SETUP.md`

## 💡 Подсказка

Самый быстрый способ начать:

```bash
# Локально
cp frontend/env.example frontend/.env
# Отредактируйте frontend/.env (замените your_* на реальные значения)
cd frontend && npm run dev

# Или для продакшена
cp env.prod.example .env.prod
# Отредактируйте .env.prod (замените your_* на реальные значения)
docker-compose -f docker-compose.prod.yml --env-file .env.prod up -d --build
```

---

**Нужна помощь?** Смотрите подробные инструкции в файлах выше или проверьте консоль браузера/логи Docker для диагностики проблем.




