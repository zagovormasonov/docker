# Система логирования административных действий

## Обзор

Система логирования отслеживает все действия администраторов в панели управления, включая модерацию статей, событий, управление пользователями и другие административные операции.

## Возможности

✅ **Полное логирование действий**
- Одобрение и отклонение статей
- Одобрение и отклонение событий
- Удаление контента
- Блокировка/разблокировка пользователей
- Создание и обновление записей

✅ **Детальная информация**
- Кто выполнил действие (имя администратора)
- Когда было выполнено действие (дата и время)
- Что было сделано (тип действия)
- Над чем было выполнено действие (тип и название объекта)
- IP-адрес и User-Agent администратора
- Дополнительные детали (например, причина отклонения)

✅ **Удобный интерфейс**
- Таблица с фильтрацией и пагинацией
- Статистика по действиям
- Быстрый поиск по типам действий и объектам
- Экспорт данных

## Установка

### 1. Создайте таблицу в базе данных

Выполните SQL-скрипт:

```bash
psql -U your_username -d your_database -f CREATE-ADMIN-LOGS-TABLE.sql
```

Или выполните вручную в pgAdmin/psql.

### 2. Перезапустите backend

```bash
cd backend
npm run dev
```

## Использование

### Доступ к логам

1. Войдите как администратор
2. Перейдите в **Админ-панель**
3. Нажмите кнопку **"📊 Логи действий"**

### Фильтрация логов

Вы можете фильтровать логи по:
- **Типу действия**: Одобрение, Отклонение, Удаление и т.д.
- **Типу объекта**: Статьи, События, Пользователи, Комментарии

### Просмотр статистики

На странице логов отображается:
- Общее количество действий
- Количество одобрений
- Количество отклонений
- Количество удалений
- Топ администраторов по активности
- Активность за последние 7 дней

## API Endpoints

### Получение логов

```http
GET /api/admin/logs
```

**Query параметры:**
- `actionType` - Фильтр по типу действия (approve, reject, delete, update, create, ban, unban)
- `entityType` - Фильтр по типу объекта (article, event, user, comment)
- `entityId` - Фильтр по ID объекта
- `adminId` - Фильтр по ID администратора
- `limit` - Количество записей (по умолчанию 50)
- `offset` - Смещение для пагинации

**Ответ:**
```json
{
  "logs": [
    {
      "id": 1,
      "admin_id": 5,
      "admin_name": "Иван Иванов",
      "action_type": "approve",
      "entity_type": "article",
      "entity_id": 123,
      "entity_title": "Название статьи",
      "details": null,
      "ip_address": "192.168.1.1",
      "user_agent": "Mozilla/5.0...",
      "created_at": "2025-11-10T12:00:00Z"
    }
  ],
  "total": 156,
  "limit": 50,
  "offset": 0
}
```

### Получение статистики

```http
GET /api/admin/logs/stats
```

**Ответ:**
```json
{
  "actionStats": [
    { "action_type": "approve", "count": "45" },
    { "action_type": "reject", "count": "12" }
  ],
  "entityStats": [
    { "entity_type": "article", "count": "35" },
    { "entity_type": "event", "count": "22" }
  ],
  "topAdmins": [
    { "admin_id": 5, "admin_name": "Иван Иванов", "actions_count": "78" }
  ],
  "recentActivity": [
    { "date": "2025-11-10", "actions_count": "15" }
  ]
}
```

## Типы действий

| Тип | Описание | Цвет |
|-----|----------|------|
| `approve` | Одобрение контента | 🟢 Зелёный |
| `reject` | Отклонение контента | 🔴 Красный |
| `delete` | Удаление контента | 🔴 Красный |
| `update` | Обновление записи | 🔵 Синий |
| `create` | Создание записи | 🟢 Зелёный |
| `ban` | Блокировка пользователя | 🔴 Красный |
| `unban` | Разблокировка пользователя | 🟢 Зелёный |

## Типы объектов

| Тип | Описание | Цвет |
|-----|----------|------|
| `article` | Статья | 🔵 Синий |
| `event` | Событие | 🟣 Фиолетовый |
| `user` | Пользователь | 🟠 Оранжевый |
| `comment` | Комментарий | 🔵 Голубой |

## Структура базы данных

### Таблица `admin_logs`

| Колонка | Тип | Описание |
|---------|-----|----------|
| `id` | SERIAL | Уникальный ID записи |
| `admin_id` | INTEGER | ID администратора |
| `admin_name` | VARCHAR(255) | Имя администратора |
| `action_type` | VARCHAR(50) | Тип действия |
| `entity_type` | VARCHAR(50) | Тип объекта |
| `entity_id` | INTEGER | ID объекта |
| `entity_title` | TEXT | Название объекта |
| `details` | JSONB | Дополнительные детали |
| `ip_address` | VARCHAR(50) | IP-адрес |
| `user_agent` | TEXT | User agent браузера |
| `created_at` | TIMESTAMP | Время создания |

### Индексы

- `idx_admin_logs_admin_id` - для быстрого поиска по администратору
- `idx_admin_logs_action_type` - для фильтрации по типу действия
- `idx_admin_logs_entity_type` - для фильтрации по типу объекта
- `idx_admin_logs_entity_id` - для поиска по ID объекта
- `idx_admin_logs_created_at` - для сортировки по дате
- `idx_admin_logs_entity_type_id` - комбинированный индекс

## Код интеграции

### Добавление логирования в новый endpoint

```typescript
import { logAdminAction } from '../utils/adminLogger';

router.post('/some-admin-action', authenticateToken, requireAdmin, async (req: AuthRequest, res) => {
  try {
    // Получаем информацию до выполнения действия
    const info = await query('SELECT title, name FROM ...');
    
    // Выполняем действие
    await query('UPDATE ...');
    
    // Логируем действие
    await logAdminAction({
      adminId: req.userId!,
      adminName: info.rows[0].name,
      actionType: 'approve', // или 'reject', 'delete' и т.д.
      entityType: 'article', // или 'event', 'user' и т.д.
      entityId: parseInt(id),
      entityTitle: info.rows[0].title,
      details: { reason: 'Какая-то причина' }, // опционально
      req: req // для получения IP и User-Agent
    });
    
    res.json({ message: 'Успешно' });
  } catch (error) {
    res.status(500).json({ error: 'Ошибка сервера' });
  }
});
```

## Примеры использования

### Поиск всех действий конкретного администратора

```typescript
GET /api/admin/logs?adminId=5&limit=100
```

### Поиск всех отклонённых статей

```typescript
GET /api/admin/logs?actionType=reject&entityType=article
```

### Получение последних 10 действий

```typescript
GET /api/admin/logs?limit=10&offset=0
```

## Безопасность

- ✅ Доступ к логам только для администраторов
- ✅ Логи нельзя удалить или изменить через API
- ✅ IP-адреса и User-Agent сохраняются для аудита
- ✅ Все действия логируются автоматически

## Рекомендации

1. **Регулярно проверяйте логи** для выявления подозрительной активности
2. **Архивируйте старые логи** (старше 6 месяцев) в отдельную таблицу
3. **Настройте уведомления** о критичных действиях (массовое удаление, банирование)
4. **Проводите ревью** логов минимум раз в неделю

## Troubleshooting

### Логи не сохраняются

1. Проверьте, что таблица `admin_logs` создана
2. Проверьте права доступа к таблице
3. Проверьте логи сервера на наличие ошибок

### Страница логов не открывается

1. Проверьте, что вы вошли как администратор
2. Проверьте, что роут `/admin-logs` зарегистрирован в `App.tsx`
3. Проверьте консоль браузера на наличие ошибок

### Статистика не обновляется

1. Проверьте endpoint `/api/admin/logs/stats`
2. Убедитесь, что в таблице есть данные
3. Проверьте права доступа к таблице

## Файлы проекта

### Backend
- `backend/src/utils/adminLogger.ts` - утилита для логирования
- `backend/src/routes/adminLogs.ts` - API endpoints для логов
- `backend/src/routes/moderation.ts` - обновлён с логированием
- `backend/src/server.ts` - регистрация роутов

### Frontend
- `frontend/src/pages/AdminLogsPage.tsx` - страница просмотра логов
- `frontend/src/pages/AdminPanel.tsx` - добавлена кнопка логов
- `frontend/src/App.tsx` - зарегистрирован роут

### SQL
- `CREATE-ADMIN-LOGS-TABLE.sql` - создание таблицы и индексов

## Поддержка

Если у вас возникли вопросы или проблемы, проверьте:
1. Логи сервера (`backend`)
2. Консоль браузера (DevTools)
3. Структуру базы данных
4. Права доступа пользователя

---

**Версия:** 1.0.0  
**Дата:** 10 ноября 2025  
**Автор:** System Administrator

