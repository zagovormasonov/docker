# 📋 Инструкция: Автоматическое истечение подписок и закрепление статей

## 🎯 Обзор функций

### 1. ⏰ Автоматическое истечение подписок
- Месячная подписка действует 30 дней после оплаты
- Годовая подписка действует 365 дней после оплаты
- Автоматическое снятие статуса эксперта после истечения срока
- Уведомления пользователям об окончании подписки

### 2. 📌 Закрепление статей на главной
- До 3 статей можно закрепить вверху списка
- Управление порядком закрепленных статей (1, 2, 3)
- Только администраторы могут закреплять статьи

---

## 🚀 Установка

### Шаг 1: Выполните миграции БД

**Для подписок:**
```bash
psql -U postgres -d your_database -f ADD-SUBSCRIPTION-EXPIRATION.sql
```

**Для закрепленных статей:**
```bash
psql -U postgres -d your_database -f ADD-PINNED-ARTICLES.sql
```

### Шаг 2: Пересоберите backend

```bash
cd backend
npm run build
```

### Шаг 3: Перезапустите Docker контейнеры

```bash
docker-compose down
docker-compose up -d --build
```

---

## 📖 Использование

### 🔄 Управление подписками

#### 1. Проверка своей подписки (для пользователей)

**API запрос:**
```
GET /api/subscriptions/my-subscription
Authorization: Bearer <token>
```

**Ответ:**
```json
{
  "user_type": "expert",
  "subscription_plan": "monthly",
  "subscription_expires_at": "2025-12-30T12:00:00Z",
  "last_payment_date": "2025-11-30T12:00:00Z",
  "days_left": 30,
  "status": "active"
}
```

#### 2. Проверка истекших подписок (только для админов)

**API запрос:**
```
POST /api/subscriptions/check-expired
Authorization: Bearer <admin-token>
```

**Ответ:**
```json
{
  "message": "Проверка завершена",
  "revokedCount": 2,
  "revokedUsers": [
    {
      "id": 123,
      "email": "user@example.com",
      "username": "User Name"
    }
  ]
}
```

#### 3. Получение списка истекающих подписок (только для админов)

**API запрос:**
```
GET /api/subscriptions/expiring?days=7
Authorization: Bearer <admin-token>
```

**Параметры:**
- `days` - за сколько дней до истечения показывать (по умолчанию 7)

**Ответ:**
```json
{
  "count": 5,
  "subscriptions": [
    {
      "id": 123,
      "email": "user@example.com",
      "username": "User Name",
      "subscription_plan": "monthly",
      "subscription_expires_at": "2025-12-05T12:00:00Z",
      "days_left": 5
    }
  ]
}
```

#### 4. Автоматическая проверка

Подписки проверяются автоматически:
- При каждом входе пользователя (но не чаще раз в 5 минут)
- Истекшие подписки отзываются автоматически
- Пользователи получают уведомления

---

### 📌 Управление закрепленными статьями

#### 1. Получить список закрепленных статей (для всех)

**API запрос:**
```
GET /api/admin/pinned-articles
```

**Ответ:**
```json
[
  {
    "id": 45,
    "title": "Важная статья",
    "content": "...",
    "is_pinned": true,
    "pin_order": 1,
    "pinned_at": "2025-11-30T12:00:00Z",
    "pinned_by_name": "Admin Name",
    "author_name": "Author Name",
    "views": 1500,
    "likes_count": 85
  }
]
```

#### 2. Закрепить статью (только для админов)

**API запрос:**
```
POST /api/admin/pinned-articles/45/pin
Authorization: Bearer <admin-token>
Content-Type: application/json

{
  "pinOrder": 1  // опционально, порядок 1-3
}
```

**Ответ:**
```json
{
  "message": "Статья успешно закреплена",
  "articleId": 45,
  "pinOrder": 1
}
```

#### 3. Открепить статью (только для админов)

**API запрос:**
```
POST /api/admin/pinned-articles/45/unpin
Authorization: Bearer <admin-token>
```

**Ответ:**
```json
{
  "message": "Статья успешно откреплена",
  "articleId": 45
}
```

#### 4. Изменить порядок закрепленной статьи (только для админов)

**API запрос:**
```
PUT /api/admin/pinned-articles/45/reorder
Authorization: Bearer <admin-token>
Content-Type: application/json

{
  "newOrder": 2
}
```

**Ответ:**
```json
{
  "message": "Порядок успешно изменен",
  "articleId": 45,
  "oldOrder": 1,
  "newOrder": 2
}
```

#### 5. Получение статей с закрепленными вверху

При обычном запросе статей закрепленные автоматически показываются вверху:

**API запрос:**
```
GET /api/articles?sort=new
```

**Ответ:**
```json
[
  {
    "id": 45,
    "title": "Закрепленная статья 1",
    "is_pinned": true,
    "pin_order": 1,
    ...
  },
  {
    "id": 52,
    "title": "Закрепленная статья 2",
    "is_pinned": true,
    "pin_order": 2,
    ...
  },
  {
    "id": 10,
    "title": "Обычная статья",
    "is_pinned": false,
    ...
  }
]
```

---

## 🛠️ Функции SQL

### Подписки

#### Проверить истекшие подписки (SQL)
```sql
SELECT * FROM check_expired_subscriptions();
```

#### Автоматически снять статус с истекших подписок (SQL)
```sql
SELECT * FROM revoke_expired_subscriptions();
```

### Закрепленные статьи

#### Получить закрепленные статьи (SQL)
```sql
SELECT * FROM get_pinned_articles();
```

#### Закрепить статью (SQL)
```sql
SELECT * FROM pin_article(
  45,  -- ID статьи
  1    -- ID администратора
);
```

#### Открепить статью (SQL)
```sql
SELECT * FROM unpin_article(
  45,  -- ID статьи
  1    -- ID администратора
);
```

---

## 🎨 Интеграция с фронтендом

### Компонент для отображения информации о подписке

```tsx
// components/SubscriptionInfo.tsx
import React, { useEffect, useState } from 'react';
import axios from 'axios';

export const SubscriptionInfo = () => {
  const [subscription, setSubscription] = useState(null);

  useEffect(() => {
    const fetchSubscription = async () => {
      try {
        const token = localStorage.getItem('token');
        const response = await axios.get('/api/subscriptions/my-subscription', {
          headers: { Authorization: `Bearer ${token}` }
        });
        setSubscription(response.data);
      } catch (error) {
        console.error('Ошибка загрузки подписки:', error);
      }
    };

    fetchSubscription();
  }, []);

  if (!subscription) return null;

  return (
    <div className="subscription-info">
      <h3>Ваша подписка</h3>
      <p>План: {subscription.subscription_plan === 'monthly' ? 'Месячная' : 'Годовая'}</p>
      <p>Осталось дней: {subscription.days_left}</p>
      <p>Истекает: {new Date(subscription.subscription_expires_at).toLocaleDateString('ru-RU')}</p>
      {subscription.days_left < 7 && (
        <div className="warning">
          ⚠️ Ваша подписка скоро истечет! Продлите её, чтобы продолжить работу.
        </div>
      )}
    </div>
  );
};
```

### Компонент для управления закрепленными статьями (админка)

```tsx
// components/admin/PinnedArticlesManager.tsx
import React, { useEffect, useState } from 'react';
import axios from 'axios';

export const PinnedArticlesManager = () => {
  const [pinnedArticles, setPinnedArticles] = useState([]);
  const [allArticles, setAllArticles] = useState([]);

  const fetchPinnedArticles = async () => {
    const response = await axios.get('/api/admin/pinned-articles');
    setPinnedArticles(response.data);
  };

  const pinArticle = async (articleId: number) => {
    try {
      const token = localStorage.getItem('token');
      await axios.post(`/api/admin/pinned-articles/${articleId}/pin`, {}, {
        headers: { Authorization: `Bearer ${token}` }
      });
      fetchPinnedArticles();
      alert('Статья закреплена!');
    } catch (error) {
      alert('Ошибка: ' + error.response?.data?.error);
    }
  };

  const unpinArticle = async (articleId: number) => {
    try {
      const token = localStorage.getItem('token');
      await axios.post(`/api/admin/pinned-articles/${articleId}/unpin`, {}, {
        headers: { Authorization: `Bearer ${token}` }
      });
      fetchPinnedArticles();
      alert('Статья откреплена!');
    } catch (error) {
      alert('Ошибка: ' + error.response?.data?.error);
    }
  };

  useEffect(() => {
    fetchPinnedArticles();
  }, []);

  return (
    <div className="pinned-articles-manager">
      <h2>Закрепленные статьи ({pinnedArticles.length}/3)</h2>
      
      <div className="pinned-list">
        {pinnedArticles.map(article => (
          <div key={article.id} className="pinned-article">
            <span>#{article.pin_order}</span>
            <h3>{article.title}</h3>
            <button onClick={() => unpinArticle(article.id)}>
              📌 Открепить
            </button>
          </div>
        ))}
      </div>

      {pinnedArticles.length < 3 && (
        <div className="pin-new">
          <h3>Закрепить статью</h3>
          {/* Список всех статей для закрепления */}
        </div>
      )}
    </div>
  );
};
```

---

## ⚠️ Важные замечания

### Подписки
1. **Существующие эксперты**: При выполнении миграции всем текущим экспертам автоматически устанавливается годовая подписка на 1 год вперед
2. **Lifetime подписки**: Если нужна бессрочная подписка, установите `subscription_expires_at = NULL`
3. **Проверка**: Подписки проверяются автоматически каждые 5 минут при активности пользователей
4. **Уведомления**: Пользователи получают уведомление об истечении подписки

### Закрепленные статьи
1. **Максимум 3 статьи**: Система автоматически не даст закрепить больше 3 статей
2. **Только опубликованные**: Закрепить можно только опубликованные и не архивированные статьи
3. **Порядок**: Закрепленные статьи автоматически показываются вверху списка в порядке pin_order (1, 2, 3)
4. **Права**: Только администраторы могут закреплять/откреплять статьи

---

## 🐛 Устранение неполадок

### Подписки не проверяются автоматически
1. Убедитесь, что сервер перезапущен после обновления
2. Проверьте логи на наличие ошибок: `docker logs synergy-backend`
3. Вручную запустите проверку через API (с правами админа)

### Ошибка при закреплении статей
1. Убедитесь, что миграция `ADD-PINNED-ARTICLES.sql` выполнена успешно
2. Проверьте, что у вас есть права администратора
3. Проверьте, что не закреплено уже 3 статьи

### Поля не найдены в БД
Выполните миграции повторно:
```bash
psql -U postgres -d your_database -f ADD-SUBSCRIPTION-EXPIRATION.sql
psql -U postgres -d your_database -f ADD-PINNED-ARTICLES.sql
```

---

## 📞 Поддержка

Если у вас возникли вопросы или проблемы, проверьте:
1. Логи Docker: `docker logs synergy-backend`
2. Логи PostgreSQL: `docker logs synergy-postgres`
3. Структуру таблиц: выполните `\d users` и `\d articles` в psql

---

## ✅ Готово!

Теперь у вас настроено:
- ✅ Автоматическое истечение подписок через месяц/год
- ✅ Уведомления об истечении подписок
- ✅ Возможность закреплять 3 статьи на главной
- ✅ Управление порядком закрепленных статей

Приятного использования! 🚀



















