# 📅 Система бронирования и записи к экспертам

## Описание

Реализована полнофункциональная система бронирования с календарём для записи клиентов к экспертам. Система включает:

- ✅ Управление доступными датами и временем для экспертов
- ✅ Календарь бронирования для клиентов
- ✅ Система подтверждения/отклонения записей
- ✅ Уведомления в чат о записях
- ✅ Отображение занятых слотов красным цветом
- ✅ История записей

## 🎯 Основные возможности

### Для эксперта:
1. **Добавление доступных слотов времени** через личный кабинет
2. **Просмотр всех записей** с фильтрацией по статусам
3. **Подтверждение или отклонение записей** с указанием причины
4. **Получение уведомлений** в чат при новой записи
5. **Управление слотами** - удаление свободных слотов

### Для клиента:
1. **Просмотр доступных слотов** на странице эксперта
2. **Бронирование записи** с возможностью добавить сообщение
3. **Отслеживание статуса записи** в разделе "Мои записи"
4. **Получение уведомлений** о подтверждении/отклонении
5. **Отмена записи** при необходимости

## 🗂️ Структура файлов

### Backend

```
backend/src/
├── config/
│   └── database.ts                    # Обновлено: добавлены таблицы для бронирования
├── routes/
│   └── bookings.ts                    # НОВЫЙ: API для бронирований
├── utils/
│   └── bookingNotifications.ts        # НОВЫЙ: Уведомления о бронировании
└── server.ts                          # Обновлено: подключен новый роут
```

### Frontend

```
frontend/src/
├── components/
│   ├── ExpertCalendar.tsx             # НОВЫЙ: Календарь для эксперта
│   ├── ExpertCalendar.css             # НОВЫЙ: Стили календаря эксперта
│   ├── ClientBookingCalendar.tsx      # НОВЫЙ: Календарь бронирования для клиента
│   ├── ClientBookingCalendar.css      # НОВЫЙ: Стили календаря клиента
│   └── Header.tsx                     # Обновлено: добавлена ссылка "Мои записи"
├── pages/
│   ├── ProfilePage.tsx                # Обновлено: добавлен ExpertCalendar
│   ├── ExpertProfilePage.tsx          # Обновлено: добавлен ClientBookingCalendar
│   ├── MyBookingsPage.tsx             # НОВЫЙ: Страница "Мои записи"
│   └── MyBookingsPage.css             # НОВЫЙ: Стили страницы записей
└── App.tsx                            # Обновлено: добавлен роут /my-bookings
```

### База данных

```
CREATE-BOOKINGS-TABLES.sql             # SQL миграция для создания таблиц
```

## 🗄️ Структура базы данных

### Таблица `expert_availability`
Хранит доступные слоты времени для записи к экспертам.

```sql
CREATE TABLE expert_availability (
  id SERIAL PRIMARY KEY,
  expert_id INTEGER REFERENCES users(id) ON DELETE CASCADE,
  date DATE NOT NULL,
  time_slot VARCHAR(50) NOT NULL,
  is_booked BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(expert_id, date, time_slot)
);
```

### Таблица `bookings`
Хранит записи клиентов к экспертам.

```sql
CREATE TABLE bookings (
  id SERIAL PRIMARY KEY,
  availability_id INTEGER REFERENCES expert_availability(id) ON DELETE CASCADE,
  expert_id INTEGER REFERENCES users(id) ON DELETE CASCADE,
  client_id INTEGER REFERENCES users(id) ON DELETE CASCADE,
  date DATE NOT NULL,
  time_slot VARCHAR(50) NOT NULL,
  status VARCHAR(20) CHECK (status IN ('pending', 'confirmed', 'rejected', 'cancelled')) DEFAULT 'pending',
  client_message TEXT,
  rejection_reason TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## 🔌 API Endpoints

### Для эксперта

#### Получить доступные слоты
```
GET /api/bookings/expert/availability
Authorization: Bearer {token}
```

#### Добавить слоты
```
POST /api/bookings/expert/availability
Authorization: Bearer {token}
Content-Type: application/json

{
  "date": "2024-12-25",
  "timeSlots": ["10:00", "11:00", "14:00"]
}
```

#### Удалить слот
```
DELETE /api/bookings/expert/availability/:id
Authorization: Bearer {token}
```

#### Получить все брони
```
GET /api/bookings/expert/bookings
Authorization: Bearer {token}
```

#### Подтвердить/отклонить бронь
```
PUT /api/bookings/expert/bookings/:id/status
Authorization: Bearer {token}
Content-Type: application/json

{
  "status": "confirmed", // или "rejected"
  "rejectionReason": "Причина отклонения" // обязательно при rejected
}
```

### Для клиента

#### Получить доступные слоты эксперта
```
GET /api/bookings/expert/:expertId/availability?startDate=2024-12-01&endDate=2024-12-31
Authorization: Bearer {token}
```

#### Забронировать слот
```
POST /api/bookings/book
Authorization: Bearer {token}
Content-Type: application/json

{
  "availabilityId": 123,
  "expertId": 456,
  "clientMessage": "Хочу проконсультироваться по вопросу..."
}
```

#### Получить свои брони
```
GET /api/bookings/my-bookings
Authorization: Bearer {token}
```

#### Отменить свою бронь
```
PUT /api/bookings/my-bookings/:id/cancel
Authorization: Bearer {token}
```

## 🚀 Установка и запуск

### 1. Обновление базы данных

Таблицы создаются автоматически при запуске сервера через `initDatabase()`.

Или можно применить миграцию вручную:
```bash
psql -U your_username -d your_database -f CREATE-BOOKINGS-TABLES.sql
```

### 2. Перезапуск backend

```bash
cd backend
npm run dev
```

### 3. Перезапуск frontend

```bash
cd frontend
npm run dev
```

## 📱 Использование

### Для эксперта:

1. Войдите в **Мой профиль**
2. Прокрутите до раздела **"📅 Управление записями"**
3. На вкладке **"🗓️ Доступные даты"**:
   - Выберите дату
   - Выберите временные слоты
   - Нажмите "Добавить выбранные слоты"
4. На вкладке **"📋 Записи"**:
   - Просматривайте ожидающие записи
   - Подтверждайте или отклоняйте их
   - Просматривайте подтвержденные и прошедшие записи

### Для клиента:

1. Перейдите на **страницу эксперта**
2. Прокрутите до раздела **"📅 Запись к эксперту"**
3. Выберите доступную дату и время
4. Добавьте сообщение (необязательно)
5. Подтвердите запись
6. Отслеживайте статус в разделе **"Мои записи"** (меню → Мои записи)

## 🔔 Уведомления

### Эксперт получает уведомление в чат когда:
- Клиент создает новую запись
- Клиент отменяет запись

### Клиент получает уведомление в чат когда:
- Эксперт подтверждает запись
- Эксперт отклоняет запись

## 🎨 Визуальные особенности

### Статусы записей:
- 🟡 **Pending** (Ожидает) - оранжевая граница
- 🟢 **Confirmed** (Подтверждено) - зеленая граница
- 🔴 **Rejected** (Отклонено) - красная граница
- ⚫ **Cancelled** (Отменено) - серая граница

### Цветовая индикация слотов:
- 🟢 **Свободно** - зеленый фон
- 🔴 **Занято** - красный фон (недоступно для бронирования)

### Адаптивный дизайн:
- ✅ Полностью адаптивен для мобильных устройств
- ✅ Поддержка темной темы
- ✅ Современный UI с градиентами и анимациями

## ⚙️ Конфигурация

### Временные слоты
По умолчанию доступны следующие слоты (можно изменить в `ExpertCalendar.tsx`):

```typescript
const TIME_SLOTS = [
  '09:00', '10:00', '11:00', '12:00', '13:00', '14:00',
  '15:00', '16:00', '17:00', '18:00', '19:00', '20:00'
];
```

### Валидация
- Нельзя добавить слоты на прошедшую дату
- Нельзя удалить забронированный слот
- Нельзя забронировать слот на прошедшую дату
- Нельзя забронировать запись у самого себя

## 🔐 Безопасность

- ✅ Все endpoints защищены JWT аутентификацией
- ✅ Проверка прав доступа (эксперт может управлять только своими слотами)
- ✅ Валидация данных на backend
- ✅ SQL injection защита через параметризованные запросы
- ✅ Проверка уникальности слотов

## 🐛 Troubleshooting

### Таблицы не созданы
```bash
# Проверьте логи сервера при запуске
# Или примените SQL миграцию вручную
psql -U your_username -d your_database -f CREATE-BOOKINGS-TABLES.sql
```

### Уведомления не приходят
- Проверьте, что Socket.IO подключен
- Проверьте, что пользователи в одном чате
- Проверьте консоль браузера на ошибки

### Слоты не отображаются
- Проверьте, что эксперт добавил доступные слоты
- Проверьте фильтр по датам (показываются только будущие даты)
- Проверьте network в DevTools

## 📝 TODO (возможные улучшения)

- [ ] Интеграция с календарём Google/Outlook
- [ ] Email уведомления о записях
- [ ] Напоминания за день до записи
- [ ] Повторяющиеся слоты (еженедельно)
- [ ] Массовое добавление слотов
- [ ] Экспорт записей в PDF
- [ ] Видеозвонки прямо из записи
- [ ] Отзывы после консультации

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи сервера
2. Проверьте консоль браузера
3. Проверьте подключение к БД
4. Обратитесь в техподдержку через раздел "Поддержка" в меню

---

**Версия:** 1.0.0  
**Дата:** 16 ноября 2025  
**Автор:** AI Assistant  
**Статус:** ✅ Готово к использованию

