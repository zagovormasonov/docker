# ⚡ Быстрое решение за 3 шага

## Проблема
Кнопка "Поделиться профилем" копирует `/experts/21` вместо `/experts/имя`

## Решение

### 1️⃣ Выполнить SQL-скрипт

```bash
psql -U postgres -d synergy -f ADD-USER-SLUG-FIELD.sql
```

**Что это делает:**
- Добавляет поле `slug` в таблицу users
- Создает функции транслитерации
- Генерирует slug для всех пользователей

### 2️⃣ Перезапустить backend

```bash
cd backend
npm run dev
```

### 3️⃣ ПЕРЕЛОГИНИТЬСЯ на сайте

1. Откройте сайт
2. Нажмите "Выйти"  
3. Войдите снова

**Это важно!** При новом входе загрузится slug.

## ✅ Проверка

1. Откройте "Мой профиль"
2. Нажмите "Поделиться профилем"
3. Вставьте из буфера

**Должно быть:**
```
https://soulsynergy.ru/experts/ваше-имя
```

## 🆘 Если не работает

Выполните проверку:

```sql
-- Подключитесь к базе
psql -U postgres -d synergy

-- Проверьте свой slug (замените 21 на ваш ID)
SELECT id, name, slug FROM users WHERE id = 21;
```

**Если slug = NULL:**
```sql
-- Обновите slug
UPDATE users 
SET slug = LOWER(REGEXP_REPLACE(
  TRANSLATE(name, 
    'абвгдеёжзийклмнопрстуфхцчшщъыьэюяАБВГДЕЁЖЗИЙКЛМНОПРСТУФХЦЧШЩЪЫЬЭЮЯ ',
    'abvgdeyozhziyklmnoprstufhtsChshschyeyuyaABVGDEYOZhZIYKLMNOPRSTUFHTsChShSchYEYuYa-'
  ),
  '[^a-z0-9-]', '-', 'g'
))
WHERE id = 21;

-- Проверьте
SELECT id, name, slug FROM users WHERE id = 21;
```

Затем **перелогиньтесь** на сайте!

---

**Подробная инструкция:** `СРОЧНОЕ-ИСПРАВЛЕНИЕ-SLUG.md`








