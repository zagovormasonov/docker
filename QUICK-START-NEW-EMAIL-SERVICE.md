# 🚀 Быстрый старт: Переход на новый EmailJS сервис

## ✅ Что уже сделано

Все файлы конфигурации обновлены для использования нового сервиса `service_22cecl9`:
- ✅ ENV-EXAMPLE.txt
- ✅ PRODUCTION-SETUP.md
- ✅ Созданы шаблоны .env файлов

## 📝 Что нужно сделать (выберите свой вариант)

### Вариант A: Локальная разработка

**Шаг 1:** Создайте файл `.env` в папке `frontend/`

```bash
# Windows PowerShell
Copy-Item frontend/env.example frontend/.env

# Linux/Mac
cp frontend/env.example frontend/.env
```

**Шаг 2:** Откройте `frontend/.env` и замените значения:

```env
VITE_EMAILJS_SERVICE_ID=service_22cecl9
VITE_EMAILJS_PUBLIC_KEY=ваш_публичный_ключ
VITE_EMAILJS_VERIFICATION_TEMPLATE_ID=ваш_template_id_для_регистрации
VITE_EMAILJS_RESET_PASSWORD_TEMPLATE_ID=ваш_template_id_для_восстановления_пароля
VITE_EMAILJS_MODERATION_TEMPLATE_ID=ваш_template_id_для_модерации
```

**Шаг 3:** Перезапустите dev сервер:

```bash
cd frontend
npm run dev
```

### Вариант B: Продакшн с Docker

**Шаг 1:** Создайте файл `.env.prod` в корне проекта

```bash
# Windows PowerShell
Copy-Item env.prod.example .env.prod

# Linux/Mac
cp env.prod.example .env.prod
```

**Шаг 2:** Откройте `.env.prod` и замените все значения `your_*` на реальные

Особенно важно:
```env
VITE_EMAILJS_SERVICE_ID=service_22cecl9  # Уже установлен
VITE_EMAILJS_PUBLIC_KEY=ваш_публичный_ключ
VITE_EMAILJS_VERIFICATION_TEMPLATE_ID=ваш_template_id
VITE_EMAILJS_RESET_PASSWORD_TEMPLATE_ID=ваш_template_id
```

**Шаг 3:** Пересоберите и запустите Docker

```bash
# Остановите текущие контейнеры
docker-compose -f docker-compose.prod.yml down

# Пересоберите с новыми переменными
docker-compose -f docker-compose.prod.yml --env-file .env.prod build --no-cache frontend

# Запустите
docker-compose -f docker-compose.prod.yml --env-file .env.prod up -d
```

## 🧪 Проверка

1. **Регистрация:**
   - Откройте страницу регистрации
   - Зарегистрируйтесь с новым email
   - Проверьте, что письмо пришло

2. **Восстановление пароля:**
   - Откройте страницу входа
   - Нажмите "Забыли пароль?"
   - Введите email и проверьте, что письмо пришло

## 📍 Где используется новый сервис

Новый сервис `service_22cecl9` автоматически используется в:
- `frontend/src/pages/RegisterPage.tsx:21` - регистрация
- `frontend/src/pages/ForgotPasswordPage.tsx:19` - восстановление пароля

## ❓ Часто задаваемые вопросы

**Q: Нужно ли менять ID шаблонов?**  
A: Нет, ID шаблонов остаются прежними. Меняется только Service ID.

**Q: Где взять эти значения?**  
A: В вашем аккаунте EmailJS (emailjs.com):
- Service ID: уже установлен `service_22cecl9`
- Public Key: Account → API Keys
- Template IDs: Email Templates → выберите шаблон → ID

**Q: Что делать если письма не приходят?**  
A: Проверьте:
1. Правильность всех переменных в .env
2. Что переменные загружены (перезапустите сервер/Docker)
3. Логи браузера (F12 → Console)
4. Квоту EmailJS (возможно исчерпан лимит)

**Q: Нужно ли что-то менять в коде?**  
A: Нет! Код автоматически использует переменные окружения.

## 🆘 Помощь

Если что-то не работает:

1. **Проверьте переменные:**
```bash
# В коде браузера (F12 → Console):
console.log(import.meta.env.VITE_EMAILJS_SERVICE_ID);

# Должно вывести: service_22cecl9
```

2. **Проверьте Docker:**
```bash
docker exec synergy-frontend env | grep VITE_EMAILJS
```

3. **Посмотрите логи:**
```bash
# Frontend
docker logs synergy-frontend

# Browser console (F12)
```

## 📚 Дополнительные материалы

- Полная инструкция: `EMAILJS-SERVICE-UPDATE.md`
- Настройка продакшена: `PRODUCTION-SETUP.md`
- Пример конфигурации: `ENV-EXAMPLE.txt`






