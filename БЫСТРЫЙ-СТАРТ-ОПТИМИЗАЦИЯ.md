# 🚀 Быстрый старт: Оптимизация проверки подписок

## ✅ Что изменилось

**Было:** Проверка каждые 5 минут (высокая нагрузка)  
**Стало:** Проверка 1 раз в день в 03:00 + предупреждения за 5 дней

---

## 📦 Установка (3 шага)

### Шаг 1: Установите node-cron

```bash
cd backend
npm install node-cron@^3.0.3
npm install --save-dev @types/node-cron@^3.0.11
```

### Шаг 2: Перезапустите backend

```bash
# Через Docker
docker-compose restart backend

# Или локально
cd backend
npm run build
npm start
```

### Шаг 3: Проверьте логи

```bash
docker logs synergy-backend | grep "CRON"
```

**Должны увидеть:**
```
⏰ ЗАПУСК CRON JOB ДЛЯ ПРОВЕРКИ ПОДПИСОК
⏰ Расписание: каждый день в 03:00
✅ Cron job для проверки подписок запущен успешно
```

---

## ⏰ Как работает

### Каждый день в 03:00 автоматически:

1. **Отправка предупреждений** (за 5 дней до истечения)
   - Пользователи получают уведомление заранее
   - Не чаще 1 раза в 24 часа

2. **Отзыв истекших подписок**
   - Автоматическое снятие статуса эксперта
   - Отправка уведомлений

---

## 🧪 Тестирование

### Ручной запуск проверки (для админа):

```bash
# Полная проверка
curl -X POST http://localhost:5000/api/subscriptions/daily-check \
  -H "Authorization: Bearer ADMIN_TOKEN"

# Только предупреждения
curl -X POST http://localhost:5000/api/subscriptions/send-warnings \
  -H "Authorization: Bearer ADMIN_TOKEN"

# Только истекшие
curl -X POST http://localhost:5000/api/subscriptions/check-expired \
  -H "Authorization: Bearer ADMIN_TOKEN"
```

---

## ⚙️ Настройки (опционально)

### Изменить время проверки

В `backend/src/utils/subscription-cron.ts`:

```typescript
// Было: 03:00
cronJob = cron.schedule('0 0 3 * * *', ...

// Изменить на 02:00
cronJob = cron.schedule('0 0 2 * * *', ...

// Или на 04:00
cronJob = cron.schedule('0 0 4 * * *', ...
```

### Изменить часовой пояс

```typescript
cronJob = cron.schedule('0 0 3 * * *', async () => {
  // ...
}, {
  scheduled: true,
  timezone: "Europe/Moscow" // Ваш часовой пояс
});
```

### Изменить период предупреждений

В `backend/src/routes/subscription-checker.ts`:

```typescript
// Было: за 5 дней
INTERVAL '5 days'

// Изменить на 7 дней
INTERVAL '7 days'

// Или на 3 дня
INTERVAL '3 days'
```

---

## 📊 Преимущества

| Параметр | Было | Стало |
|----------|------|-------|
| Проверок в день | ~288 | 1 |
| Нагрузка на БД | Высокая | Минимальная |
| Предупреждения | Нет | За 5 дней |
| Экономия ресурсов | - | 99.7% |

---

## 🎯 Готово!

Теперь система работает оптимально:
- ✅ Минимальная нагрузка
- ✅ Предупреждения заранее
- ✅ Автоматические проверки ночью

**Подробная документация:** `ОПТИМИЗАЦИЯ-ПРОВЕРКИ-ПОДПИСОК.md`
