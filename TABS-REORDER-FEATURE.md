# Функционал перетаскивания табов в профиле

## Описание
Добавлена возможность изменять порядок вкладок "Фото" и "Галерея" в профиле эксперта с помощью drag-and-drop (перетаскивания мышью) с автоматическим сохранением настроек в базе данных.

## Реализованные функции

### 1. Отображение счетчиков элементов
- ✅ Счетчики отображаются всегда (даже до клика на вкладку)
- ✅ Данные загружаются сразу при открытии профиля из API
- ✅ Формат: `Фото (4)`, `Галерея (2)`

### 2. Drag-and-Drop перетаскивание вкладок
- ✅ Полноценное перетаскивание мышью (drag-and-drop)
- ✅ Доступно только владельцу профиля
- ✅ Визуальные эффекты при перетаскивании:
  - Иконка "⋮⋮" для индикации возможности перетаскивания
  - Курсор меняется на "grab" при наведении
  - Вкладка поворачивается и получает тень при перетаскивании
  - Фон области меняется при наведении перетаскиваемого элемента
- ✅ Визуальная подсказка: "💡 Перетащите вкладки мышью, чтобы изменить их порядок"
- ✅ Автоматическое сохранение в базу данных

### 3. Бэкенд изменения

#### База данных
- Добавлено поле `tabs_order` в таблицу `users` (тип JSONB)
- Значение по умолчанию: `["photos", "gallery"]`

#### API эндпоинты
- `GET /api/users/me` — возвращает `tabsOrder`
- `PUT /api/users/profile` — принимает `tabsOrder` для сохранения
- `GET /api/experts/:id` — возвращает `tabs_order`, `galleryCount`, `artworksCount`

### 4. Фронтенд изменения

#### Новые зависимости
```bash
npm install react-beautiful-dnd @types/react-beautiful-dnd
```

#### Компоненты
- `ExpertProfilePage.tsx`:
  - Состояние `tabsOrder` для хранения порядка
  - Функция `handleTabsReorder` для сохранения
  - Динамическое построение вкладок на основе `tabsOrder`
  - Кнопки быстрого переключения порядка

## Использование

### Для владельца профиля
1. Откройте свой профиль эксперта
2. Прокрутите до секции с вкладками "Фото" и "Галерея"
3. **Перетащите вкладку мышью** в нужное место:
   - Наведите курсор на вкладку (курсор изменится на "grab")
   - Зажмите левую кнопку мыши
   - Перетащите вкладку влево или вправо
   - Отпустите кнопку мыши
4. Порядок сохраняется автоматически

### Для посетителей
- Видят вкладки в том порядке, который установил владелец профиля
- Счетчики элементов отображаются сразу
- Перетаскивание недоступно (только просмотр)

## Технические детали

### Структура данных
```typescript
// В базе данных (PostgreSQL JSONB)
tabs_order: ["photos", "gallery"] | ["gallery", "photos"]

// На фронтенде
const [tabsOrder, setTabsOrder] = useState<string[]>(['photos', 'gallery']);
```

### API запросы
```typescript
// Сохранение порядка
await api.put('/users/profile', { tabsOrder: ['gallery', 'photos'] });

// Получение профиля с порядком
const response = await api.get(`/experts/${id}`);
// response.data.tabs_order, response.data.galleryCount, response.data.artworksCount
```

## Будущие улучшения
- [x] ~~Добавить drag-and-drop для перетаскивания мышью~~ ✅ Реализовано
- [ ] Поддержка большего количества вкладок
- [ ] Анимация плавного перехода при изменении порядка
- [ ] Возможность скрывать вкладки
- [ ] Сохранение настроек отображения для разных устройств

## Миграция базы данных
При первом запуске после обновления:
1. Автоматически добавится колонка `tabs_order` в таблицу `users`
2. Для всех существующих пользователей будет установлено значение по умолчанию
3. Никаких дополнительных действий не требуется
