# 📤 Модальное окно "Поделиться" в "Мой профиль"

## ✨ Что добавлено

Теперь в разделе **"Мой профиль"** при нажатии на кнопку **"Поделиться профилем"** открывается такое же красивое модальное окно, как в профиле эксперта!

### Было:
- При нажатии просто копировалась ссылка
- Не было выбора действий
- Не показывалась карточка профиля

### Стало:
- ✅ Открывается модальное окно с карточкой
- ✅ 3 варианта действий:
  - **Поделиться** - нативное меню (WhatsApp, Telegram, etc.)
  - **Копировать ссылку** - только URL
  - **Копировать всё** - полная информация с контактами
- ✅ Красивая визуализация профиля
- ✅ Единообразный UX с профилями экспертов

## 🎨 Что в модалке

```
┌──────────────────────────┐
│  [Градиентный фон]       │
│ ┌────────────────────┐   │
│ │   🙂 Аватар        │   │
│ │                    │   │
│ │  Ваше имя          │   │
│ │  📍 Город          │   │
│ │  Описание...       │   │
│ │                    │   │
│ │  🎯 Направления    │   │
│ │  [тег] [тег]       │   │
│ │                    │   │
│ │  📞 Контакты       │   │
│ │  📱 Telegram       │   │
│ │  📱 WhatsApp       │   │
│ │  ──────────────    │   │
│ │  🔗 ссылка со slug │   │
│ │  ──────────────    │   │
│ │  [📤 Поделиться]   │   │
│ │  [📋] [📋 Всё]     │   │
│ └────────────────────┘   │
└──────────────────────────┘
```

## 📝 Изменённые файлы

**`frontend/src/pages/ProfilePage.tsx`**

### 1. Добавлен импорт:
```typescript
import ShareProfileModal from '../components/ShareProfileModal';
```

### 2. Добавлено состояние:
```typescript
const [shareModalVisible, setShareModalVisible] = useState(false);
```

### 3. Изменён обработчик кнопки:
```typescript
// Было:
onClick={async () => {
  await navigator.clipboard.writeText(profileUrl);
  message.success('Ссылка скопирована!');
}}

// Стало:
onClick={() => setShareModalVisible(true)}
```

### 4. Добавлен рендер модалки:
```typescript
{user && (
  <ShareProfileModal
    visible={shareModalVisible}
    onClose={() => setShareModalVisible(false)}
    expert={{
      id: user.id,
      name: user.name,
      slug: user.slug,
      avatar_url: user.avatarUrl,
      bio: user.bio,
      city: user.city,
      topics: user.topics || [],
      telegram_url: user.telegramUrl,
      whatsapp: user.whatsapp
    }}
  />
)}
```

## 🔄 Маппинг полей

Поля из `user` (camelCase) преобразуются в формат `expert` (snake_case):

| User (ProfilePage) | Expert (ShareModal) |
|-------------------|---------------------|
| `user.id` | `expert.id` |
| `user.name` | `expert.name` |
| `user.slug` | `expert.slug` |
| `user.avatarUrl` | `expert.avatar_url` |
| `user.bio` | `expert.bio` |
| `user.city` | `expert.city` |
| `user.topics` | `expert.topics` |
| `user.telegramUrl` | `expert.telegram_url` |
| `user.whatsapp` | `expert.whatsapp` |

## 🚀 Как работает

### 1. Пользователь открывает "Мой профиль"
### 2. Видит кнопку "Поделиться профилем" под email
### 3. Нажимает на кнопку
### 4. Открывается модальное окно с карточкой
### 5. Выбирает действие:
   - **Поделиться** → открывается нативное меню ОС
   - **Копировать ссылку** → `https://soulsynergy.ru/experts/ваш-slug`
   - **Копировать всё** → полная информация:

```
🌟 Ваше имя

Ваше описание...

📍 Ваш город

🎯 Направления:
• Направление 1
• Направление 2

📞 Контакты:
📱 Telegram: https://t.me/you
📱 WhatsApp: +79123456789

🔗 Профиль: https://soulsynergy.ru/experts/ваш-slug

SoulSynergy - пространство совместного духовного развития
```

## ✅ Преимущества

1. **Единообразный UX** - одинаковое поведение в своём профиле и чужом
2. **Больше опций** - не просто копирование, а выбор действия
3. **Красивая визуализация** - видно, что именно делишься
4. **Мобильная оптимизация** - Web Share API на телефонах
5. **Удобство** - все контакты в одном месте

## 🧪 Тестирование

### Проверьте:

1. **Desktop:**
   - Откройте "Мой профиль"
   - Нажмите "Поделиться профилем"
   - Открывается модалка ✅
   - Все кнопки работают ✅
   - Slug в ссылке ✅

2. **Mobile:**
   - Кнопка "Поделиться" открывает нативное меню ✅
   - Можно отправить в Telegram, WhatsApp ✅
   - Fallback на копирование работает ✅

3. **Данные:**
   - Аватар отображается ✅
   - Имя, город, описание корректны ✅
   - Направления (тематики) отображаются ✅
   - Контакты (Telegram, WhatsApp) показаны ✅
   - Ссылка со slug ✅

## 🎯 Итог

### Было:
```typescript
onClick={() => {
  clipboard.writeText(url);
  message.success('Скопировано!');
}}
```

### Стало:
```typescript
onClick={() => setShareModalVisible(true)}
// → Открывается модалка с 3 опциями!
```

**Теперь функционал "Поделиться профилем" одинаковый везде!** 🎉

---

**Дата:** 28 ноября 2025  
**Готово к использованию!** ✨

