# 🚀 Быстрый старт: Красивые URL для экспертов

## ✨ Что нового?

1. **Красивые URL** - `/experts/ivan-ivanov` вместо `/experts/123`
2. **Кнопка "Поделиться"** - легко делиться профилем
3. **Лучший placeholder** - понятнее, что писать в "О себе"

---

## 📋 Шаги установки

### 1️⃣ Обновить базу данных

```bash
# Выполнить SQL-скрипт
psql -U postgres -d synergy -f ADD-USER-SLUG-FIELD.sql
```

**Что произойдёт:**
- ✅ Добавится поле `slug` в таблицу `users`
- ✅ Создадутся функции транслитерации
- ✅ Автоматически сгенерируются slug для всех пользователей

### 2️⃣ Перезапустить backend

```bash
cd backend
npm run dev
```

### 3️⃣ Перезапустить frontend

```bash
cd frontend
npm run dev
```

### 4️⃣ Проверить!

1. Откройте страницу экспертов
2. Кликните на любого эксперта
3. Проверьте URL - должен быть slug, например: `/experts/ivan-ivanov`
4. Откройте "Мой профиль"
5. Найдите кнопку "Поделиться профилем" под email
6. Нажмите - ссылка скопирована! ✅

---

## ✅ Быстрая проверка

### SQL-запрос:
```sql
-- Проверить slug у экспертов
SELECT id, name, slug 
FROM users 
WHERE user_type IN ('expert', 'admin')
ORDER BY id
LIMIT 10;
```

**Ожидаемый результат:**
```
 id |        name        |       slug
----+--------------------+-------------------
  1 | Иван Иванов        | ivan-ivanov
  2 | Мария Петрова      | maria-petrova
  3 | Алексей Смирнов    | aleksey-smirnov
```

---

## 🎯 Что изменилось?

### Backend:
- ✅ `backend/src/utils/transliterate.ts` - новый файл
- ✅ `backend/src/routes/experts.ts` - обновлён
- ✅ `backend/src/routes/auth.ts` - обновлён

### Frontend:
- ✅ `frontend/src/pages/ExpertsPage.tsx` - обновлён
- ✅ `frontend/src/pages/FavoritesPage.tsx` - обновлён
- ✅ `frontend/src/pages/ProfilePage.tsx` - обновлён

---

## 🐛 Если что-то не работает

### Кнопка "Поделиться" не копирует ссылку
```bash
# Проверьте консоль браузера (F12)
# Убедитесь, что используете HTTPS или localhost
```

### Ссылки с slug не открываются
```sql
-- Проверьте, что slug существует
SELECT slug FROM users WHERE id = YOUR_ID;

-- Если NULL, сгенерируйте заново
UPDATE users 
SET slug = generate_unique_slug(name, id) 
WHERE id = YOUR_ID;
```

### Backend выдаёт ошибку
```bash
# Проверьте, что файл transliterate.ts создан
ls backend/src/utils/transliterate.ts

# Перезапустите backend
cd backend
npm run dev
```

---

## 📖 Подробная документация

См. файл `УЛУЧШЕНИЯ-ПРОФИЛЕЙ-ЭКСПЕРТОВ.md`

---

**Готово! Наслаждайтесь красивыми URL! 🎉**


