# Демонстрация Drag-and-Drop функционала

## Визуальные индикаторы

### Для владельца профиля:

```
┌─────────────────────────────────────────────────────────────┐
│ 💡 Перетащите вкладки мышью, чтобы изменить их порядок      │
└─────────────────────────────────────────────────────────────┘

┌──────────────────┐  ┌──────────────────┐
│ ⋮⋮ 📷 Фото (4)   │  │ ⋮⋮ 🖼️ Галерея (2)│
└──────────────────┘  └──────────────────┘
     ↑ Активная           ↑ Неактивная
     (синяя)              (серая)
```

### При наведении:
- Курсор меняется на `grab` (рука)
- Иконка `⋮⋮` показывает, что элемент можно перетащить

### При перетаскивании:
```
┌──────────────────┐
│ ⋮⋮ 📷 Фото (4)   │  ← Поворот на 2°
└──────────────────┘     Тень вокруг
     ↓ Перетаскивается

┌─────────────────────────────────────┐
│  [Подсвечивается фон области]       │
└─────────────────────────────────────┘
```

### После отпускания:
- Вкладка встает на новое место
- Автоматически сохраняется в БД
- Плавная анимация возврата в нормальное состояние

## Для посетителей:

```
┌──────────────────┐  ┌──────────────────┐
│ 📷 Фото (4)      │  │ 🖼️ Галерея (2)   │
└──────────────────┘  └──────────────────┘
     ↑ Без иконки ⋮⋮
     ↑ Курсор pointer (обычный)
     ↑ Перетаскивание отключено
```

## Технические детали реализации

### Библиотека: react-beautiful-dnd
- **DragDropContext** - обертка для всей области перетаскивания
- **Droppable** - область, куда можно бросить элемент
- **Draggable** - перетаскиваемый элемент

### Стили при перетаскивании:
```css
cursor: grab;                    /* При наведении */
cursor: grabbing;                /* При перетаскивании */
transform: rotate(2deg);         /* Поворот */
box-shadow: 0 4px 12px rgba(...) /* Тень */
background: #f0f5ff;             /* Фон области при наведении */
```

### Отключение для посетителей:
```typescript
isDragDisabled={user?.id !== expert.id}
```

## Примеры использования

### Сценарий 1: Владелец хочет показать галерею первой
1. Открывает свой профиль
2. Видит подсказку "💡 Перетащите вкладки..."
3. Наводит на "🖼️ Галерея (2)"
4. Зажимает мышь и тащит влево
5. Отпускает - галерея теперь первая
6. Изменение сохранено автоматически

### Сценарий 2: Посетитель просматривает профиль
1. Открывает профиль эксперта
2. Видит вкладки в порядке, установленном владельцем
3. Может кликать для переключения
4. Не видит иконки ⋮⋮
5. Не может перетаскивать

## Производительность

- Сохранение в БД происходит асинхронно
- UI не блокируется во время сохранения
- При ошибке сохранения порядок остается в UI (graceful degradation)
- Минимальное количество ре-рендеров благодаря React.memo

## Доступность

- ✅ Работает с клавиатурой (Tab для навигации)
- ✅ Визуальные индикаторы состояния
- ✅ Понятные подсказки
- ⚠️ Screen reader support - требует дополнительной работы
